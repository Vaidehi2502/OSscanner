# Copy to frontend/.env.local (already gitignored) and restart `npm start`
# to apply - Create React App only reads env files at startup and only
# exposes vars prefixed REACT_APP_ to the browser bundle.

# Must exactly match the backend's API_KEY, if one is set. Leave empty if
# the backend is running without authentication.
REACT_APP_API_KEY=
